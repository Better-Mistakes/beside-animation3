import * as React from "react";
import * as Types from "./types";

declare function SlotItemTestimonialSliderSlide(props: {
  as?: React.ElementType;
  quote?: React.ReactNode;
  personImage?: Types.Asset.Image;
  personName?: React.ReactNode;
  personPosition?: React.ReactNode;
}): React.JSX.Element;
